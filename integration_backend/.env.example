# Example environment variables for integration_backend
# Copy to .env and adjust as needed. Do not commit real secrets.

# Server
HOST=0.0.0.0
PORT=3001
LOG_LEVEL=INFO
DEV_MODE=true

# CORS - comma-separated list of allowed origins
# When cookies/credentials are used, wildcard "*" is invalid; set explicit origins.
ALLOWED_ORIGINS=http://localhost:3000,https://vscode-internal-12731-beta.beta01.cloud.kavia.ai:3000

# Database (defaults to sqlite file if unset)
# INTEGRATION_DB_URL=sqlite:///./integration.db

# Atlassian/Jira OAuth
JIRA_OAUTH_CLIENT_ID=
JIRA_OAUTH_CLIENT_SECRET=
# Must exactly match Atlassian console setting
JIRA_REDIRECT_URI=https://your-host:3001/auth/jira/callback

ATLASSIAN_CLOUD_BASE_URL=https://your-team.atlassian.net
APP_FRONTEND_URL=http://localhost:3000

# Security
STATE_SIGNING_SECRET=replace-with-random
CSRF_SECRET=replace-with-random
SECRET_KEY=replace-with-random

# Optional: Confluence OAuth (if using a distinct app)
# CONFLUENCE_OAUTH_CLIENT_ID=
# CONFLUENCE_OAUTH_CLIENT_SECRET=
# CONFLUENCE_OAUTH_REDIRECT_URI=
