# Integration Backend (.env.example)
# Copy to .env and fill with your values. For cloud previews, use absolute public URLs.

# Atlassian OAuth (PKCE)
ATLASSIAN_CLIENT_ID=
# Optional but supported in token/refresh requests:
ATLASSIAN_CLIENT_SECRET=
# Must exactly match Atlassian developer console redirect URL (use backend public URL; do NOT use localhost in cloud):
# Example: https://your-backend.example.com/api/oauth/callback/atlassian
ATLASSIAN_REDIRECT_URI=
# Space-separated list; if omitted, sensible defaults are applied (includes offline_access)
ATLASSIAN_SCOPES=read:jira-work read:jira-user read:confluence-content.all read:confluence-space.summary offline_access

# Public URLs (cloud preview domains)
# Backend absolute public URL (no trailing slash), e.g., https://vscode-internal-xxxxx.cloud.kavia.ai:3001
BACKEND_BASE_URL=
# Frontend absolute public URL (no trailing slash), e.g., https://vscode-internal-xxxxx.cloud.kavia.ai:3000
FRONTEND_BASE_URL=

# CORS configuration (comma-separated origins)
# Include your frontend origin for browser calls with credentials.
BACKEND_CORS_ORIGINS=

# Database (defaults to local SQLite if not provided)
# INTEGRATION_DB_URL=postgresql+psycopg://user:password@host:5432/dbname
