# Example environment configuration for integration_backend
# Copy to .env and adjust values for your environment.
# PUBLIC_INTERFACE
# Jira/Atlassian OAuth 2.0 (3LO)
JIRA_OAUTH_CLIENT_ID=your_atlassian_client_id
JIRA_OAUTH_CLIENT_SECRET=your_atlassian_client_secret

# Preferred redirect URI variable for Jira OAuth. Must match Atlassian app settings exactly.
# Default value shown here matches the expected deployment acceptance criteria.
JIRA_REDIRECT_URI=https://vscode-internal-36721-beta.beta01.cloud.kavia.ai:3001/auth/jira/callback

# Canonical/alias variables also supported (if you prefer these names):
# ATLASSIAN_OAUTH_REDIRECT_URI=
# ATLASSIAN_REDIRECT_URI=
# JIRA_OAUTH_REDIRECT_URI=

# Backend public base URL (used to construct fallback redirect when not explicitly set)
# PUBLIC_BASE_URL=https://vscode-internal-36721-beta.beta01.cloud.kavia.ai:3001

# Atlassian Cloud base (your site domain)
# ATLASSIAN_CLOUD_BASE_URL=https://your-team.atlassian.net

# Frontend base URL for post-auth UI redirect (not used for Atlassian redirect_uri)
# APP_FRONTEND_URL=https://your-frontend.example.com
