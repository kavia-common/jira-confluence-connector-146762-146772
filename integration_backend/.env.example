# Integration Backend .env example for Atlassian OAuth (PKCE)
# Copy this file to .env and fill in values.

# Required for Atlassian OAuth (PKCE)
ATLASSIAN_CLIENT_ID=your_atlassian_client_id
ATLASSIAN_REDIRECT_URI=https://<your-backend-domain>/api/oauth/callback/atlassian

# Optional (PKCE can work without secret; if provided it will be used in token/refresh)
ATLASSIAN_CLIENT_SECRET=

# Optional: scopes override (space-separated). Defaults include Jira+Confluence read + offline_access.
ATLASSIAN_SCOPES=read:jira-work read:jira-user read:confluence-content.all read:confluence-space.summary offline_access

# Public base URLs for cloud preview
BACKEND_BASE_URL=https://<your-backend-domain>
FRONTEND_BASE_URL=https://<your-frontend-domain>

# CORS (comma-separated list of origins)
BACKEND_CORS_ORIGINS=https://<your-frontend-domain>

# Database (optional; defaults to local SQLite file)
# INTEGRATION_DB_URL=sqlite:///./integration.db

# Logging
LOG_LEVEL=INFO
