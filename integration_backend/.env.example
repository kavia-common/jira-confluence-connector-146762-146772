# Integration Backend .env.example

# Environment
APP_ENV=development
DEV_MODE=true
LOG_LEVEL=INFO

# Database (SQLite by default)
INTEGRATION_DB_URL=sqlite:///./integration.db

# CORS
BACKEND_CORS_ORIGINS=http://localhost:3000

# Frontend base URL (only for post-auth redirects to UI; NOT used as OAuth redirect_uri)
APP_FRONTEND_URL=http://localhost:3000

# Atlassian site base (your tenant)
ATLASSIAN_CLOUD_BASE_URL=https://your-team.atlassian.net

# Jira OAuth client credentials
JIRA_OAUTH_CLIENT_ID=your-client-id
JIRA_OAUTH_CLIENT_SECRET=your-client-secret

# STRICT: Jira OAuth redirect URI used to construct Atlassian authorize URL.
# Must match exactly in Atlassian Developer Console.
# Example for this deployment:
# JIRA_REDIRECT_URI=https://vscode-internal-36721-beta.beta01.cloud.kavia.ai:3001/auth/jira/callback
JIRA_REDIRECT_URI=

# Optional: Public backend origin is no longer used for redirect derivation, but you may still set it for docs.
# PUBLIC_BASE_URL=https://your-backend.example.com

# Optional: enable encryption key for token storage (see docs)
# ENCRYPTION_KEY=32-byte-hex-or-base64
