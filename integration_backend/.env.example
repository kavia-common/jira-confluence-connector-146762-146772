# Backend example environment variables for local/dev
# Copy to .env and adjust values. Do not commit real secrets.
APP_ENV=development
DEV_MODE=true
LOG_LEVEL=INFO

# CORS - allow your frontend origin(s)
NEXT_PUBLIC_BACKEND_CORS_ORIGINS=https://localhost:3000

# Frontend base URL used only for redirect after successful OAuth
APP_FRONTEND_URL=https://localhost:3000

# Atlassian site base (no trailing slash)
ATLASSIAN_CLOUD_BASE_URL=https://your-team.atlassian.net

# Jira OAuth 2.0 (STRICT: redirect must match Atlassian app)
JIRA_OAUTH_CLIENT_ID=your_client_id
JIRA_OAUTH_CLIENT_SECRET=your_client_secret
JIRA_REDIRECT_URI=https://localhost:3001/auth/jira/callback

# Optional: Confluence distinct app
# CONFLUENCE_OAUTH_CLIENT_ID=
# CONFLUENCE_OAUTH_CLIENT_SECRET=
# CONFLUENCE_OAUTH_REDIRECT_URI=

# CSRF/State signing secrets (recommended to set)
APP_SECRET_KEY=change-me
STATE_SIGNING_SECRET=change-me-state

# Database (defaults to local sqlite file if unset)
# INTEGRATION_DB_URL=sqlite:///./integration.db
