# Integration Backend - Example .env
# Copy this file to .env and fill in values for your environment.

# Database
INTEGRATION_DB_URL=sqlite:///./integration.db

# Atlassian Cloud base URL (your Jira/Confluence site root)
# e.g., https://your-team.atlassian.net
ATLASSIAN_CLOUD_BASE_URL=

# Jira OAuth 2.0 (3LO)
# Required: set your Atlassian app client credentials and redirect URI.
# IMPORTANT: The redirect URI must exactly match what is configured in the Atlassian developer console.
JIRA_OAUTH_CLIENT_ID=
JIRA_OAUTH_CLIENT_SECRET=
# Set this to the provided callback URL:
# https://vscode-internal-36910-beta.beta01.cloud.kavia.ai:4000/oauth/jira
JIRA_OAUTH_REDIRECT_URI=https://vscode-internal-36910-beta.beta01.cloud.kavia.ai:4000/oauth/jira

# If you use a separate client for Confluence, set these; otherwise Jira values are reused.
CONFLUENCE_OAUTH_CLIENT_ID=
CONFLUENCE_OAUTH_CLIENT_SECRET=
CONFLUENCE_OAUTH_REDIRECT_URI=

# Frontend base URL used for post-auth redirects (backend will redirect users back here)
# e.g., http://localhost:3000 or a preview URL
APP_FRONTEND_URL=

# Optional alternate PKCE-based (API-prefixed) OAuth flow settings
# Note: These are used by src/api/oauth_settings.py for PKCE endpoints (e.g., /api/oauth/atlassian/login)
ATLASSIAN_CLIENT_ID=
ATLASSIAN_CLIENT_SECRET=
# If using PKCE flow, ensure this callback matches your app configuration:
ATLASSIAN_REDIRECT_URI=
ATLASSIAN_SCOPES=
BACKEND_BASE_URL=
FRONTEND_BASE_URL=

# CORS configuration
BACKEND_CORS_ORIGINS=http://localhost:3000
