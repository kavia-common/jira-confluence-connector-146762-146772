# Integration Backend .env (example)
# Copy to integration_backend/.env and fill in real values. Do NOT commit real secrets.

# Environment
APP_ENV=development
DEV_MODE=true
LOG_LEVEL=INFO

# Database (defaults to SQLite if omitted)
# INTEGRATION_DB_URL=sqlite:///./integration.db

# CORS origins (comma separated). In dev you can leave as *.
BACKEND_CORS_ORIGINS=*

# Frontend base URL (used to redirect after OAuth completes)
# e.g., https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3000
APP_FRONTEND_URL=https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3000

# Atlassian Cloud base URL for your site (e.g., https://your-team.atlassian.net)
ATLASSIAN_CLOUD_BASE_URL=https://your-team.atlassian.net

# Jira OAuth 2.0 (3LO) - REQUIRED
# These must be configured in your Atlassian developer app and provided here.
# Redirect URI must exactly match Atlassian app settings.
JIRA_OAUTH_CLIENT_ID=your-client-id
JIRA_OAUTH_CLIENT_SECRET=your-client-secret
# Keep this EXACT value for the current deployment as instructed:
JIRA_OAUTH_REDIRECT_URI=https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3001/auth/jira/callback

# Confluence OAuth 2.0 (optional if using separate app; falls back to Jira values if omitted)
# CONFLUENCE_OAUTH_CLIENT_ID=
# CONFLUENCE_OAUTH_CLIENT_SECRET=
# CONFLUENCE_OAUTH_REDIRECT_URI=

# Optional NEXT_PUBLIC_* fallbacks are supported (see README) but prefer server-side variables above.
