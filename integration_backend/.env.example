# Database (optional; defaults to local SQLite file)
# INTEGRATION_DB_URL=sqlite:///./integration.db

# Atlassian OAuth (PKCE 3LO)
ATLASSIAN_CLIENT_ID=
# Optional if using PKCE-only, but include if configured for your app
ATLASSIAN_CLIENT_SECRET=
# Must exactly match the redirect set in Atlassian developer console
# e.g., http://localhost:3001/api/oauth/callback/atlassian
ATLASSIAN_REDIRECT_URI=
# Space-separated scopes; default covers Jira + Confluence read + offline_access
# ATLASSIAN_SCOPES=read:jira-work read:jira-user read:confluence-content.all read:confluence-space.summary offline_access

# Application URLs
# APP_BASE_URL=http://localhost:3001
APP_FRONTEND_URL=http://localhost:3000

# CORS (comma-separated origins). Use specific origins in production.
BACKEND_CORS_ORIGINS=http://localhost:3000
