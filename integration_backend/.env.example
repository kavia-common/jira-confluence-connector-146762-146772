# Application environment
APP_ENV=production
DEV_MODE=false
LOG_LEVEL=INFO

# CORS (comma separated origins). In dev you can keep "*"
BACKEND_CORS_ORIGINS=*

# Database
# Default is local SQLite file: sqlite:///./integration.db
# INTEGRATION_DB_URL=sqlite:///./integration.db

# Atlassian Cloud base (your tenant)
# Example: https://your-team.atlassian.net
ATLASSIAN_CLOUD_BASE_URL=

# Jira OAuth (Atlassian 3LO)
# Strictly ensure this redirect URI exactly matches what is registered in Atlassian
# For this deployment, it MUST be:
# https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3001/auth/jira/callback
JIRA_OAUTH_CLIENT_ID=
JIRA_OAUTH_CLIENT_SECRET=
JIRA_OAUTH_REDIRECT_URI=https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3001/auth/jira/callback

# Optional: Confluence OAuth (if using a separate app)
CONFLUENCE_OAUTH_CLIENT_ID=
CONFLUENCE_OAUTH_CLIENT_SECRET=
CONFLUENCE_OAUTH_REDIRECT_URI=

# Frontend base URL used for post-auth redirect
# Default recommended return URL (UI route): https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3000
APP_FRONTEND_URL=https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3000

# Optional: if you want to customize scopes passed to /auth/jira/login via query
# Otherwise backend defaults to: "read:jira-work read:jira-user offline_access"
# JIRA_SCOPES=read:jira-work read:jira-user offline_access

# Alternate NEXT_PUBLIC_* fallbacks (used if present)
# NEXT_PUBLIC_ATLASSIAN_CLIENT_ID=
# NEXT_PUBLIC_ATLASSIAN_CLIENT_SECRET=
# NEXT_PUBLIC_ATLASSIAN_REDIRECT_URI=
# NEXT_PUBLIC_APP_FRONTEND_URL=
# NEXT_PUBLIC_FRONTEND_BASE_URL=
# NEXT_PUBLIC_ATLASSIAN_CLOUD_BASE_URL=
# NEXT_PUBLIC_BACKEND_CORS_ORIGINS=
