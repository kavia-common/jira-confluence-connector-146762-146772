# Integration Backend - Example .env
# Copy to integration_backend/.env and fill your values.

# App environment flags
APP_ENV=development
DEV_MODE=true
LOG_LEVEL=INFO

# CORS (comma-separated). In dev you can leave "*"
# BACKEND_CORS_ORIGINS=https://your-frontend.example.com,https://another.example.com

# Frontend base URL used for post-auth redirect (UI route appended server-side)
# The backend will redirect to: <APP_FRONTEND_URL>/oauth/jira?status=success&...
APP_FRONTEND_URL=https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3000

# Atlassian Cloud base URL for your site (used for data placeholders and storage)
# Example: https://your-team.atlassian.net
ATLASSIAN_CLOUD_BASE_URL=https://your-team.atlassian.net

# ========= Jira OAuth 2.0 (3LO) =========
# These three MUST be provided and redirect URI MUST exactly match Atlassian app settings.
JIRA_OAUTH_CLIENT_ID=your_jira_oauth_client_id
JIRA_OAUTH_CLIENT_SECRET=your_jira_oauth_client_secret
# IMPORTANT: Must match exactly what is registered in Atlassian.
# For this deployment template, the path should be /auth/jira/callback (without /api) unless your proxy keeps /api.
JIRA_OAUTH_REDIRECT_URI=https://vscode-internal-13311-beta.beta01.cloud.kavia.ai:3001/auth/jira/callback

# Optional fallbacks accepted by backend (if you already have these in your environment)
# ATLASSIAN_CLIENT_ID=...
# ATLASSIAN_CLIENT_SECRET=...
# ATLASSIAN_REDIRECT_URI=...

# ========= Confluence OAuth (optional separate app) =========
# If not set, the backend will fall back to Jira/Atlassian values.
# CONFLUENCE_OAUTH_CLIENT_ID=your_confluence_oauth_client_id
# CONFLUENCE_OAUTH_CLIENT_SECRET=your_confluence_oauth_client_secret
# CONFLUENCE_OAUTH_REDIRECT_URI=https://your-backend.example.com/auth/confluence/callback

# ========= Database =========
# Default is SQLite file ./integration.db if not provided
# INTEGRATION_DB_URL=sqlite:///./integration.db
