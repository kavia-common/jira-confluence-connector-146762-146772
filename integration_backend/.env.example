# Backend environment example for FastAPI service

# Canonical Atlassian redirect URIs (Jira required)
# MUST MATCH Atlassian app configuration exactly.
JIRA_REDIRECT_URI=https://vscode-internal-36200-beta.beta01.cloud.kavia.ai:3001/auth/jira/callback

# Atlassian OAuth credentials
JIRA_OAUTH_CLIENT_ID=
JIRA_OAUTH_CLIENT_SECRET=

# Optional distinct Confluence OAuth credentials (fallbacks to Jira if omitted)
CONFLUENCE_OAUTH_CLIENT_ID=
CONFLUENCE_OAUTH_CLIENT_SECRET=
# Optional explicit Confluence redirect; if not set, Jira redirect above is used
CONFLUENCE_OAUTH_REDIRECT_URI=

# Optional: Frontend base URL used only for post-auth UI redirect (not Atlassian redirect_uri)
APP_FRONTEND_URL=https://vscode-internal-36200-beta.beta01.cloud.kavia.ai:3000

# CORS
BACKEND_CORS_ORIGINS=https://vscode-internal-36200-beta.beta01.cloud.kavia.ai:3000

# Database connection (example)
INTEGRATION_DB_URL=sqlite:///./integration.db

# Misc
DEV_MODE=true
LOG_LEVEL=INFO
