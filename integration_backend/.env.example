# Backend environment example for Jira/Confluence connector

# FastAPI logging
LOG_LEVEL=INFO
APP_ENV=production
DEV_MODE=false

# CORS - comma-separated origins, use exact scheme://host:port
# Example: https://vscode-internal-12731-beta.beta01.cloud.kavia.ai:3000
BACKEND_CORS_ORIGINS=

# Database (defaults to SQLite file if not set)
# INTEGRATION_DB_URL=postgresql+psycopg://user:password@host:5432/dbname

# Frontend base URL used for post-auth redirects
# Example: https://vscode-internal-12731-beta.beta01.cloud.kavia.ai:3000
APP_FRONTEND_URL=

# Atlassian Cloud base (optional)
ATLASSIAN_CLOUD_BASE_URL=

# Jira OAuth credentials (required)
JIRA_OAUTH_CLIENT_ID=
JIRA_OAUTH_CLIENT_SECRET=

# Strict redirect URI configured in Atlassian developer console
# Must exactly match, e.g., https://<backend-host>:3001/auth/jira/callback
JIRA_REDIRECT_URI=

# Optional: State signing secret for OAuth state cookie
STATE_SIGNING_SECRET=

# Optional cookie settings
JIRA_STATE_COOKIE_NAME=jira_oauth_state
JIRA_STATE_COOKIE_TTL_SEC=600
