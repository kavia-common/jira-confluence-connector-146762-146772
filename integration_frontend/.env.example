# Integration Frontend (.env.example)
# Copy to .env.local and fill your cloud preview backend URL.
# This value must be the publicly accessible backend base URL (no trailing slash).
NEXT_PUBLIC_BACKEND_URL=https://<your-backend-preview-domain>
